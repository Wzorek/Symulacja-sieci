#ifndef NET_SIMULATION_TYPES_H
#define NET_SIMULATION_TYPES_H

typedef int ElementID;
typedef int Time;
typedef int TimeOffset;
enum class ReceiverType { WORKER, STOREHOUSE };
enum class QueueType { LIFO, FIFO };

#endif //NET_SIMULATION_TYPES_H
